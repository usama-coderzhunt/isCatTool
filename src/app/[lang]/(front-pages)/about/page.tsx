import { SystemMode } from '@/@core/types'
import AboutUsWrapper from '@/views/front-pages/about/page'

const AboutPage = ({ mode }: any) => {
  return <AboutUsWrapper mode={mode} />
}

export default AboutPage
